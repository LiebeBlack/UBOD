name: CI

on:
  push:
    branches: [main, master]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Build & test (Linux)
    runs-on: ubuntu-latest
    # La suite E2E levanta servidores mTLS en loopback: si algo se atasca, es
    # preferible fallar en minutos que agotar el límite de 6 h del workflow
    # (una suite colgada no deja ningún diagnóstico útil).
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Instalar toolchain estable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache de cargo
        uses: Swatinem/rust-cache@v2

      - name: Formato
        run: cargo fmt --all -- --check

      - name: Clippy (warnings = errores)
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Tests completos (incluye E2E mTLS)
        run: cargo test --workspace

      - name: Build release
        run: cargo build --workspace --release

      - name: Subir binarios
        uses: actions/upload-artifact@v4
        with:
          name: vault-binaries-linux
          path: |
            target/release/vaultd
            target/release/vaultctl
            target/release/vault-gui
            target/release/vault-it
          if-no-files-found: error
